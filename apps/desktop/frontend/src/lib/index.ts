// place files you want to import through the `$lib` alias in this folder.
//
export const SERVICES: {
  [key: string]: string;
} = {
  rcfile: "Receive files",
};

export const NAMES = ["DESKTOP", "MOBILE", "TV", "WEB"];
export const ICONS = ["🖳", "📱", "📺", "🕸"];
